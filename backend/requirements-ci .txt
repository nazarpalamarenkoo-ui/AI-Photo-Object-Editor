-r requirements-base.txt

# ML — CPU build (CI only, no GPU on GitHub-hosted runners)
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1